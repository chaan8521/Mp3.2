import React from 'react';

const CurrencyResult = ({ result }) => {
  return (
    <div id="result" className="mt-4 text-center text-lg">
      {result}
    </div>
  );
};

export default CurrencyResult;
